<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.vkas.safekey">

    <application
        android:name=".application.App"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="false"
        android:resizeableActivity="false"
        tools:ignore="UnusedAttribute"
        tools:replace="android:icon,android:supportsRtl"
        android:theme="@style/Theme.AppTheme">
        <meta-data
            android:name="com.google.android.gms.ads.APPLICATION_ID"
            android:value="ca-app-pub-2174740078812392~7263824144" />
        <meta-data android:name="com.facebook.sdk.ApplicationId" android:value="@string/facebook_app_id"/>

        <activity
            android:name=".ui.start.StartupActivity"
            android:theme="@style/SplashTheme"
            android:launchMode="singleTop"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".ui.main.MainActivity"
            android:launchMode="singleTask"/>
        <activity android:name=".ui.list.SelectActivity"
            android:launchMode="singleTop"/>
        <activity android:name=".ui.result.ResultActivity"
            android:launchMode="singleTop"/>
        <activity android:name=".ui.web.PrivacyPolicyActivity"
            android:launchMode="singleTop"/>
    </application>

</manifest>